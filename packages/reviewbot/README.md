# reviewbot

LLM-based review interface accepting code diffs and structured findings to review the diff.

## Usage

`reviewbot` is an internal library consumed by `evalkit`. Its primary entry point is the `review` function exported from concrete implementation of the `Reviewer` interface.
